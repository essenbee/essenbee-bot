@page
@model Essenbee.Bot.Web.Pages.Project.CreateModel

@{
    ViewData["Title"] = "Create";
}

<h2>Create</h2>

<h4>Project Text</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        @if (Model.ProjectText == null)
        {
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="ProjectText.Text" class="control-label"></label>
                <input asp-for="ProjectText.Text" class="form-control" />
                <span asp-validation-for="ProjectText.Text" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </form>
        }
    </div>
</div>

<div>
    <a asp-page="/Index">Back to Admin</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
